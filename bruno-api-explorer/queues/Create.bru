meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: {{BASE_URL_V1}}/queues
  body: json
  auth: inherit
}

body:json {
  {
    "name":"test",
    "channels":[""]
  }
}

settings {
  encodeUrl: true
}
